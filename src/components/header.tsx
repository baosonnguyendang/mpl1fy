import React from 'react';
import PropTypes from 'prop-types';

const header = (props: any) => {
    return (
        <div>
            
        </div>
    );
};

// header.propTypes = {
    
// };

export default header;